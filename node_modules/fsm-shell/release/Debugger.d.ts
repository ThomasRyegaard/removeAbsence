import { EventType } from './ShellEvents';
import { EventDirection } from './models/debug/debug-event';
interface Routing {
    to?: string[];
    from?: string[];
}
export declare class Debugger {
    private winRef;
    private debugId;
    private debugMode;
    constructor(winRef: Window, debugId: string);
    traceEvent(direction: EventDirection, type: EventType, payload: any, routing: Routing, hasHandler: boolean): void;
    private logEvent;
}
export {};
