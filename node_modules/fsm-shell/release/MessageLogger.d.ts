import { DebugEvent, EventDirection } from './models/debug/debug-event';
import { EventType } from './ShellEvents';
interface FilterOptions {
    type?: EventType[] | EventType;
    direction?: EventDirection;
    component: string | string[];
    handled: boolean;
    from: Date;
    to: Date;
}
export declare class MessageLogger {
    messages: DebugEvent<any>[];
    push(event: DebugEvent<any>, debugId: string): void;
    all(): DebugEvent<any>[];
    allTable(): void;
    filter(options: FilterOptions): DebugEvent<any>[];
    filterTable(options: FilterOptions): void;
}
export {};
